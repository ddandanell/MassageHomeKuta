import MassageTypeLandingPage from "@/templates/MassageTypeLandingPage";
import { balineseData } from "@/data/massageTypes";

const BalineseMassage = () => {
  return <MassageTypeLandingPage data={balineseData} />;
};

export default BalineseMassage;
