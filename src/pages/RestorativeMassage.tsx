import MassageTypeLandingPage from "@/templates/MassageTypeLandingPage";
import { restorativeMassageData } from "@/data/specialtyMassageTypes2";

const RestorativeMassage = () => {
  return <MassageTypeLandingPage data={restorativeMassageData} />;
};

export default RestorativeMassage;
