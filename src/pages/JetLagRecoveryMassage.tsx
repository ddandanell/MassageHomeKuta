import MassageTypeLandingPage from "@/templates/MassageTypeLandingPage";
import { massageTypes } from "@/data/massageTypes";

const JetLagRecoveryMassage = () => {
  return <MassageTypeLandingPage data={massageTypes.jetLag} />;
};

export default JetLagRecoveryMassage;
